Revise the gamepad system. You should probably rename it to joystick again given that not all joysticks are gamepads. Make joystick button and axis handling safe. Some joysticks might not have the same amount of buttons and axes as a gamepad.
Forward the window key events to the keyboard input system.
Forward mouse events to the mouse input system.
Modify Makefile to generate .a and .so files. Check wether validate whether .a and .so files and which arguments you'd need to pass to the compiler to do so.
Create <path_to_root>/test project. In here, you will write all your test cases for the platform project located in <path_to_root>/platform.
Implement a second framework like Win32 or Qt.
